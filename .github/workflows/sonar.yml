name: SonarQube Cloud

on:
  push:
    branches: [main, develop]

  pull_request:
    branches: [main, develop]

jobs:
  sonar:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 21
          cache: maven

      - name: Grant execute permission
        run: chmod +x mvnw

      - name: Analyze with SonarQube Cloud
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          ./mvnw clean verify

          echo "===== JACOCO FILES ====="
          ls -R target/site/jacoco

          echo "===== JACOCO XML ====="
          head -30 target/site/jacoco/jacoco.xml

          echo "===== LINE COUNTERS ====="
          grep 'counter type="LINE"' target/site/jacoco/jacoco.xml | tail

          ./mvnw sonar:sonar \
            -Dsonar.projectKey=FleetOps-Corp_FleetOps-Asignaciones-Service \
            -Dsonar.organization=fleetops-corp \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.token=$SONAR_TOKEN \
            -Dsonar.coverage.jacoco.xmlReportPaths=target/site/jacoco/jacoco.xml \
            -Dsonar.qualitygate.wait=true